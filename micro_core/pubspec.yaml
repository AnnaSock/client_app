name: micro_core
description: Routage globale et navigation centrale de la SuperApp

publish_to: 'none'
version: 0.0.1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  go_router: ^17.1.0
  base_app:
    path: ../base_app

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
