name: micro_core
description: Routage globale et navigation centrale de la SuperApp

publish_to: 'none'
version: 0.0.1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  go_router: ^17.1.0
  base_app:
    path: ../base_app
  wallet_tfs_app:
    path: ../micro_apps/wallet-tfs-app
  bceao_pi:
    path: ../micro_apps/bceao-pi
  firebase_core: ^4.6.0
  firebase_auth: ^6.3.0
  cloud_firestore: ^6.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
